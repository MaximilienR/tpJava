package org.example;

import java.util.Map;
import java.util.Scanner;

public class Logique {
    public static void Mot{
        Scanner sc = new Scanner(System.in);
        for () {
            System.out.println("Please, enter message type to check it priority: ");
            String userInput = sc.next();

        Mot[] mots1=phrase.stream()
                .flapMap(phrase->phrase.getMotList().stram);
                )} list.stream().anyMatch(str -> (str.length() == 3));
        Map<String,Mot >mot1 =phrase/toString().startsWith(
        )collect(Collectors.toMap(Produt::getMots funcition))
        System.out.println(phase);
    }
}
